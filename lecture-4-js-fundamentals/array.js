const students = [ 'Sami', 'Farmee', 'Aarishamma', 'Aaarsah', 'Nasif', 'Namira', 'Rawda', 'Ramisha' ];

for ( let i = 0; i < students.length; i++ ) {
    console.log( students[ i ], students[ i ].length, students[ i ].toLowerCase(), students[ i ].toUpperCase() );
}

// for ( const name of students ) {
//     console.log( name, name.length );
// }